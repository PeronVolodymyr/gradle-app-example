**This is a simple example of gradle based java project.**

Run this command to launch the project:
`./gradlew :app:run --console=plain`

Run this command to analyse project dependencies:
`./gradlew buildHealth`